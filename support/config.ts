const config = {
  baseUrl: "about:blank",
  recordVideos: false,
  browser: "chromium", // chromium or firefox or webkit
  defaultTimeout: 60 * 1000, // milliseconds
  runHeadless: false,
  runSlow: 0, // milliseconds
};

export default config;
